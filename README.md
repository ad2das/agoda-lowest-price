# Agoda Always Lowest Price (아고다 최저가 도우미)

전 세계 아고다 숙소에서 **2인 유효 최저가**를 찾아 비교하고, 사용자가 원할 때만 1클릭 예약을 진행하는 유저스크립트입니다.

- 한국어 / English / 日本語 페이지 모두 지원
- 일본뿐 아니라 모든 국가의 아고다 숙소에서 동작

## 기능

| 기능 | 설명 |
|---|---|
| 최저가 자동 탐지 | 페이지의 모든 객실 오퍼를 스캔해 2인 기준 유효 최저가를 찾음 |
| 저가 채널(cid) 자동 적용 | 평소에는 공개 가격도구 합집합 + 공식 앱의 47개를 안전 간격으로 비교하고, 결제 총액이 안정적으로 더 싼 CID만 실제 페이지 응답까지 확인한 뒤 자동 전환 (검색 조건별 30분 캐시) |
| 1인실/인원초과 필터 | `성인 1명`, `1 Adult`, `大人1名`, `for 1 Person` 등 1인용 요금을 자동 제외 (가짜 최저가 방지) |
| 최저가 하이라이트 | 최저가 오퍼 셀을 초록 테두리 + `⬇ 최저가` 배지로 표시 |
| 1클릭 예약 | `🎯 최저가 바로 예약` 버튼 — 최저가 셀로 스크롤 후 예약 버튼 클릭 |
| 세금포함 총액 표시 | `1박당 요금`(세금포함) × 박수(`los`)로 총액 계산. 명시된 총액 우선, 없으면 국가별 세금 계수로 추정 |
| 결제합계 쿠폰 자동비교 | 결제 페이지에서 중복 제거된 코드를 하나씩 적용·해제하고, 실제 합계가 내려간 코드만 후보로 인정한 뒤 가장 낮은 합계를 재검증해 최종 적용 |
| 쿠폰팩 실제 지갑 활성화 | 파트너 캠페인에서 추출한 17개 redeem 링크를 제공자별 할인 우선순위로 시도하고, HTTP 응답이 아니라 지갑 API의 `campaignId`/`promotionCode` 추가를 확인한 쿠폰만 성공 처리 |
| 가격 변동 감지 | 방문 중 가격이 내려가면 알림 + 관찰 최저가 기록 (localStorage) |
| 현지통화 경고 | 표시 통화가 숙소 국가 통화가 아니면 5% 수수료 위험 경고 |

## 설치

1. Firefox에 Violentmonkey 설치 (Chrome/Edge는 Tampermonkey도 지원)
2. 아래 링크의 내용 복사:
   - https://raw.githubusercontent.com/ad2das/agoda-lowest-price/main/agoda-userscript.user.js
3. Violentmonkey 대시보드 → **새 스크립트** → 붙여넣기 → 저장 (Ctrl+S)
4. 아고다 호텔 페이지를 열면 우측 상단에 패널이 나타남

## 사용법

1. 아고다에서 숙소 + 날짜 + **성인 2명**으로 검색 → 호텔 페이지 진입
2. 패널이 자동으로 최저가(1박, 세전)와 총액(세금포함)을 표시
3. **`🎯 최저가 바로 예약`** 클릭 → 최저가 오퍼로 예약 진행
4. 결제 페이지에서 스크립트가 지갑/기본 합계와 입력형 쿠폰 합계를 자동 비교 → 실제 최저 쿠폰 재적용
5. 패널 우측 상단 `—` 버튼으로 접기, 헤더 드래그로 이동

### 패널 설정

- **최저가 하이라이트** — 초록 테두리/배지 표시 여부
- 객실 예약 버튼은 자동으로 클릭하지 않습니다. 예약 진행은 **`🎯 최저가 바로 예약`**을 직접 눌렀을 때만 실행됩니다.
- **결제합계 쿠폰 자동비교** — 결제 페이지에서 실제 최종 합계가 내려간 코드만 인정하고 가장 싼 코드를 다시 적용 (기본 켜짐)
- **쿠폰팩 실제 지갑 활성화** — 숙소 페이지에서 17개 캠페인 후보를 활성화하고 지갑에 실제 들어온 항목만 확인. 새 쿠폰이 생기면 숙소 가격 반영을 위해 한 번 새로고침 (기본 켜짐)
- **가격변동 감지** — 관찰 최저가 기록 여부
- **저가 채널(cid) 자동적용** — 호텔 페이지 진입 시 채널(cid) 스캔 후 더 싼 채널로 자동 전환 (기본 켜짐, 끄면 원래 가격 유지)
- **실사용 CID 368개 검사** — 캐시를 지우고 실측 검증된 CID 368개를 사용자가 요청했을 때만 전체 검사
- **쿠폰 목록 편집** — 시도할 쿠폰 코드 추가/삭제 (한 줄에 하나)

## 동작 원리

- 객실 오퍼 가격은 `[data-selenium="PriceDisplay"]` 요소에서 파싱
- 아고다는 URL의 `cid` 파라미터(파트너 채널 번호)에 따라 **같은 호텔·같은 날짜라도 가격이 다르게** 응답합니다. 스크립트는 현재 페이지의 최신 `room-grid` 요청을 그대로 복제해 결제 총액을 비교하고, 최신 API를 사용할 수 없을 때만 `GetSecondaryData`를 호환 경로로 사용합니다.
- 숫자 범위를 무작위 대입하지 않습니다. 자동 검색은 세 공개 가격도구의 합집합 46개와 공식 Android 앱 1개를 합친 빠른 47개 목록, 현재/과거 승자, 페이지 링크와 `CampaignInfo` 응답에서 실제 관찰한 CID만 사용합니다. 패널의 수동 전체 검사는 APA 난바 에키마에 타워 조건으로 가격 응답을 실측한 368개 CID를 확인합니다.
- 전체 추출 결과와 출처는 [`agoda-cids.json`](./agoda-cids.json)에 보존합니다. 중복 제거된 368개 심층 감사는 개발자 API `__NYX_AGODA__.rescanCid('deep')`로만 실행되며, 일반 UI에서 실수로 대량 요청하지 않습니다. 인접 번호·placeholder·도구가 `Math.random()`으로 만든 번호는 후보에 넣지 않습니다.
- 자동 후보는 각 1회 확인하고 현재 CID와 가격순 상위 최대 20개만 2회 추가 조회합니다. 총 3개 표본 중 2개 이상이 0.5% 안에서 일치해야 인정되며, 순간적으로만 싸게 나온 가짜 최저는 중앙값에서 탈락합니다. 선택값보다 싸게 보였지만 안정 검증되지 않은 후보가 하나라도 남으면 자동 적용·캐시를 보류합니다.
- CID를 URL에 넣은 뒤에는 새 페이지가 자연스럽게 보낸 `ag-cid`와 실제 `room-grid`/호환 API 총액을 다시 읽고, 합성 확인 2회와 함께 검증합니다. URL만 바뀌었거나 실제 가격이 되돌아가면 해당 CID를 격리하고 다시 비교합니다.
- 캐시는 호텔 ID뿐 아니라 체크인·숙박일수·객실 수·성인/자녀 수·자녀 나이·통화를 구분합니다. 캐시를 만들 때와 다른 CID로 유입되면 캐시를 그대로 적용하지 않고 다시 비교합니다.
- 요청은 최대 2개 작업자와 최소 300ms 시작 간격으로 제한합니다. 429가 한 번이라도 감지되면 즉시 전체 검사를 중지하며, 5xx는 `Retry-After`를 따릅니다. 미응답 후보가 남으면 “차이 없음” 결과는 캐시하지 않습니다.
- 자동 첫 검사는 보통 20~30초가 필요하지만, 같은 검색 조건의 30분 캐시는 즉시 재사용됩니다. 368개 수동 전체 검사는 서버 상태에 따라 약 2분 이상 걸릴 수 있습니다.
- 채널별 가격 차이는 호텔과 검색 조건마다 다릅니다. 후보 가격이 모두 같으면 현재 URL을 그대로 유지합니다.
- 1인용 요금(예: 일본 호텔의 `1인 요금`)은 인원수 표시(`성인 1명`/`1 Adult`/`大人1名`)와 객실명(`for 1 Person`)으로 판별해 제외 — **2인 검색 기준으로 진짜 최저가만** 표시
- 세금포함 총액: 셀에 명시된 `1박당 요금`(per night) 값을 우선 사용 → 없으면 국가별 세금 계수(locale 기반, 예: 일본 26.5%)로 추정
- 가격은 아고다 서버가 내려주는 값을 그대로 사용 (임의로 조작하지 않음)

## 주의

- 가격은 아고다 서버 기준이며 할인(플래시딜 등)이 이미 적용된 값입니다
- 모든 숙소·날짜에 공통인 단 하나의 “무조건 최저 CID”는 없습니다. CID별 재고·프로모션·계정·결제수단 조건이 달라 같은 검색 조건으로 직접 비교해야 합니다
- Agoda는 개인/제휴 계정의 전체 CID 레지스트리를 공개하지 않습니다. 이 프로젝트의 “전체”는 2026-08-02 현재 공식 페이지·공식 앱·공개 실서비스 번들·공개 링크에서 증거를 확인할 수 있는 범위이며, 새 `CampaignInfo` CID는 실행 중 자동 수집됩니다
- 카드 프로모션 CID는 결제 단계에서 해당 카드나 계정 자격을 요구할 수 있습니다. 스크립트는 객실 API가 제시한 세금 포함 예약 총액을 비교하지만 결제수단 자격을 만들어내지는 않습니다
- 1인 1실 예약 시에는 1인 요금이 더 저렴할 수 있으나, 이 스크립트는 **2인 검색 기준**으로 동작합니다
- 쿠폰 코드는 호텔/계정/시기별로 적용 여부가 달라 항상 유효한 것은 아닙니다
- 비공식 유저스크립트입니다 — 아고다와 무관하며, 변경된 페이지 구조에 따라 수동 업데이트가 필요할 수 있습니다

## 버전 이력

- **v1.10.1** — 예약/결제 페이지에서도 사용자 스크립트가 부팅되도록 수정, 쿠폰팩 17개를 지갑 API로 개별 검증, 실측 할인값 표시, 입력형 쿠폰을 적용 전후 최종 합계로 비교·해제하고 가장 낮은 합계를 재검증해 적용
- **v1.9.8** — v1.9.7에서 검색 페이지를 완전 제외해 같은 탭 SPA 호텔 진입 시 CID 검색이 사라지던 회귀 수정: 검색 DOM은 건드리지 않고 대기하며 `pushState`/`replaceState` 호텔 전환 순간 CID 캡처와 지연 패널을 활성화
- **v1.9.7** — 모바일 Agoda 검색 결과가 검게 비는 충돌 수정: `/search` 페이지에서 스크립트 완전 제외, 호텔 상세 페이지의 `load` 완료 후에만 패널 생성, 상세 페이지를 벗어나면 패널 숨김
- **v1.9.6** — Violentmonkey가 오래된 v1.9.3을 받던 GitHub branch-raw 캐시를 우회하도록 update/download URL을 최신 direct raw로 교체, 패널 버전·전체 검사 개수를 실제 값과 동기화
- **v1.9.5** — APA 난바 에키마에 타워(2026-10-07~09, 성인 2명) `room-grid` 실측으로 272개 최저 티어 CID와 96개 차상위 CID를 검증하고 레지스트리 갱신
- **v1.9.4** — Agoda 페이지 보호 긴급 수정: 자동 객실 클릭 제거, 기존 위험 설정 1회 강제 비활성화, 자동 검사를 검증된 47개 단축 목록으로 축소, 전체 180개는 명시적 버튼으로만 실행, 동시 작업 2개/300ms 간격 및 429 즉시 중단, 네트워크 캡처를 숙소 페이지로 제한
- **v1.9.3** — 10개 사전 점검으로 유효 가격이 없는 최신 API를 즉시 건너뛰고 호환 API로 전환, 객실 가격 API가 준비되기 전 무응답 전수 검색 방지
- **v1.9.2** — GitHub raw CDN의 오래된 1.9.0 캐시를 피하도록 설치·업데이트 URL을 GitHub branch-raw 경로로 교체
- **v1.9.1** — Violentmonkey가 v1.9.0 설치본에서 새 배포본을 확실히 감지하도록 버전 증가, 패널에 실행 버전 표시
- **v1.9.0** — 랜덤 CID 브루트포스 제거, 현행 실사용 180개/공개 관측 241개 증거 레지스트리 도입, 런타임 `CampaignInfo` 수집, 2인 수용 불가 `cheapestPrice` 제거, 누락 CID 선택 재시도와 429·전체 5xx backoff, 상위 20개 병렬 2/3 안정 중앙값 검증, 자녀 나이 포함 조건 캐시, 새 유입 CID 재검증, 적용 후 endpoint/body가 일치하는 자연 페이지 응답·총액 확인, 실패 CID 10분 격리·원복·최대 이동 횟수 제한, SPA 재검색 큐
- **v1.8.0** — 7자리 CID 전 범위 적응형 탐색: 최대 150개 층화 표본, 시간/수동 재검색마다 새 표본, 과거 승자·인접 CID 학습, 상위 후보 2회 결제 총액 재검증, 30분 조건별 캐시
- **v1.7.0** — Firefox/Violentmonkey용 CID 검색 완성: 최신 `room-grid` API 우선 + 기존 API 폴백, 실제 결제 총액 비교, 검색 조건별 캐시, 고정 동시성/요청 간격/서버 오류 조기 중단, 반복 리디렉션 방지, CID 수동 재검색 버튼 및 디버그 상태 API 추가
- **v1.6.0** — 저가 채널(cid) 자동 적용: 호텔별 채널 가격 스캔(약 100채널 백그라운드) → 더 싼 채널 발견 시 자동 전환, 호텔별 6시간 캐시. 실제 결제 금액 반영 검증 완료 (2박 ₩273,212 → ₩258,034, ~5.6% 절약). 신라호텔 등 채널 차이 없는 호텔은 유지. 헤드리스 E2E로 cid=48000 자동 전환 확인
- **v1.5.2** — Firefox/신형 레이아웃(a6d3f) 변형 최저가 탐색 버그 수정: 노이즈(조회수·리뷰·브레드크럼) 필터, 객실카드 지문 검증(`1박당`/`총액`), 가격이 안 잡히면 상단 고정바(StickyNav) 최저가 폴백. 헤드리스 Firefox·Chrome 양쪽에서 최저 ₩106,680 / 2박 ≈ ₩269,900 검증 완료
- **v1.5.0** — 범용 가격 파서: `106,680원`, `KRW 106,680`, `106,680 KRW`, `엔`, 인도네시아 점 표기 등 모든 표기 지원 (19개 포맷 테스트 통과). data-selenium 없는 페이지 변형에서도 동작, 진단 로그에 가격 텍스트 샘플 출력
- **v1.4.0** — 범용 파서 추가 (통화기호 없는 가격, 레이아웃 변형 대응), `최저가 바로 예약` 재시도 4회, 못 찾을 시 [진단] 로그 출력, 모바일(m.agoda.com) 지원
- **v1.3.1** — 총액 파싱 버그 수정 (`1박당` 오인, night 카운트 오인), ko/en/ja 다국어 정규식 테스트 통과
- **v1.3.0** — 전 세계 숙소 지원 (다국어 인원 필터, 박수 기반 총액, 국가별 세금 계수)
- **v1.2.0** — 2인 유효 최저가 자동 선택, 1클릭 예약 버튼, 세금포함 총액 표시
- **v1.1.0** — 실제 DOM 셀렉터 기반 재작성, 조식가 오수집 버그 수정
